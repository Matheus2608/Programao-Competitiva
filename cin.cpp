#include <bits/stdc++.h>
#define endln '\n';

using namespace std;

int main() {
    string x;
    string s;
    scanf("%s", &x);
    printf("%s", x);
    getline(cin, s);
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);

    cout << s << '\n';

    while(cin >> x) {
        cout << x << '\n';
    }
    
}

