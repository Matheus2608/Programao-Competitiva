#include<bits/stdc++.h>
using namespace std;

#define FOR(a, b, c) for (int(a) = (b); (a) < (c); ++(a))
#define REP(i, n) FOR(i, 0, n)

int main(){
    long long a,b;cin>>a>>b;
    long long c = a;
    cout << "OI" << endl;
    return 0;
}